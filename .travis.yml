language: cpp
compiler:
  - clang
  - gcc
script: mkdir build && cd build && cmake .. -DSC_ENABLE_TESTING=ON && make -j3 && make -j3 test
branches:
  only:
    - master
    - /^review\/.*$/
notifications:
  irc: "chat.freenode.net#stepcode"
  email: scl-dev@groups.google.com
  on_success: change
  on_failure: always
